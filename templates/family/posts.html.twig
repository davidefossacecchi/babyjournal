{% extends 'base.html.twig' %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-lg-4 col-sm-6">
            <h2>Ciao</h2>
            {% for post in posts%}
                <div class="row">
                    <div class="col-xs-12">
                        <img src="{{ post_url(post) }}"/>
                    </div>
                </div>
            {% endfor %}
        </div>

    </div>
{% endblock %}
