{% extends 'base.html.twig' %}

{% block body %}
    {% if form %}
    <div class="row justify-content-center">
        <div class="col-lg-4 col-sm-6">
            <div class="card">
                <div class="card-body">
                    {{ form(form, {action: path('password_recovery', {t: tokenString}), method: 'POST'}) }}
                </div>
            </div>

        </div>
    </div>
    {% endif %}

{% endblock %}
