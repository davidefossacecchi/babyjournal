{% extends 'base.html.twig' %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-lg-4 col-sm-6">
            <div class="card">
                <div class="card-body">
                    {{ form(form, {action: path('password_reset'), method: 'POST'}) }}
                </div>
            </div>

        </div>

    </div>

{% endblock %}
